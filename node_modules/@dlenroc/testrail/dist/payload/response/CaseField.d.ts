import { Context } from './Context';
export interface CaseField extends Record<string, unknown> {
    configs: CaseField.CaseFieldConfig[];
    description?: string;
    display_order: number;
    id: number;
    include_all: boolean;
    is_active: boolean;
    label: string;
    name: string;
    system_name: string;
    template_ids: number[];
    type_id: number;
}
export declare namespace CaseField {
    interface CaseFieldConfig extends Record<string, unknown> {
        context: Context;
        id: string;
        options: Options;
    }
    interface Options {
        default_value?: string;
        format?: string;
        has_actual?: boolean;
        has_expected?: boolean;
        is_required: boolean;
        items?: string;
        rows?: string;
    }
}
//# sourceMappingURL=CaseField.d.ts.map